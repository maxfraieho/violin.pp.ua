# 🎻 Інструкція налаштування Telegram замовлень для violin.pp.ua

## ✅ Що вже зроблено:

1. ✅ Створено Telegram бота (TOKEN: `8242972523:AAHrhraZ2dir1Arn32nLSnPPT1RZe9XSVuQ`)
2. ✅ Підготовлено код Cloudflare Worker
3. ✅ Підготовлено оновлений JavaScript для форми
4. ✅ Підготовлено оновлений HTML для контактної секції

---

## 📝 Кроки для завершення:

### Крок 1: Отримати Chat ID Арсена (5 хвилин)

Арсену потрібно:

1. Відкрити Telegram
2. Знайти бота `@userinfobot` (або `@getmyid_bot`)
3. Написати `/start`
4. Бот покаже `Your user ID: 123456789` ← **це Chat ID**

**Або простіше:**
1. Відкрийте свого бота (через @BotFather знайдіть посилання)
2. Напишіть боту будь-що (наприклад, "Привіт")
3. Відкрийте в браузері:
   ```
   https://api.telegram.org/bot8242972523:AAHrhraZ2dir1Arn32nLSnPPT1RZe9XSVuQ/getUpdates
   ```
4. В відповіді знайдіть `"chat":{"id":123456789}` ← **це ваш Chat ID**

---

### Крок 2: Створити Cloudflare Worker (10 хвилин)

#### 2.1. Створити акаунт Cloudflare (безкоштовно)
- Перейти на https://workers.cloudflare.com/
- Зареєструватися (якщо ще немає акаунту)
- Підтвердити email

#### 2.2. Створити Worker
1. В панелі Cloudflare: **Workers & Pages** → **Create application** → **Create Worker**
2. Назвати його, наприклад: `violin-telegram-webhook`
3. **Deploy** (поки з дефолтним кодом)

#### 2.3. Додати код
1. Після деплою натиснути **Edit code**
2. Видалити весь існуючий код
3. Вставити код з артефакту `telegram_webhook` (перший що я створив)
4. **Save and Deploy**

#### 2.4. Додати Environment Variables
1. В Worker налаштуваннях: **Settings** → **Variables**
2. Додати дві змінні:
   - **Ім'я:** `TELEGRAM_BOT_TOKEN`  
     **Значення:** `8242972523:AAHrhraZ2dir1Arn32nLSnPPT1RZe9XSVuQ`  
     ☑️ Encrypt
   
   - **Ім'я:** `TELEGRAM_CHAT_ID`  
     **Значення:** `[Chat ID з Кроку 1]`  
     ☑️ Encrypt

3. **Save**

#### 2.5. Отримати URL Worker'а
Після деплою Worker матиме URL типу:
```
https://violin-telegram-webhook.YOUR_USERNAME.workers.dev
```

**Скопіюйте цей URL** - він потрібен для наступного кроку!

---

### Крок 3: Оновити файли сайту

#### 3.1. Оновити `assets/js/main.js`
1. Відкрити файл `assets/js/main.js`
2. **ЗАМІНИТИ ВСЕ** на код з артефакту `updated_booking_form`
3. В рядку 47 замінити:
   ```javascript
   const WEBHOOK_URL = 'https://YOUR_WORKER_NAME.workers.dev';
   ```
   На ваш справжній URL Worker'а:
   ```javascript
   const WEBHOOK_URL = 'https://violin-telegram-webhook.YOUR_USERNAME.workers.dev';
   ```

4. Перевірити Telegram username (рядок 46):
   ```javascript
   const TELEGRAM_USERNAME = 'arsen111999'; // Перевірте, чи це правильний username
   ```

#### 3.2. Оновити `index.html`
1. Знайти секцію `<!--contact-->` (близько рядка 370)
2. Замінити ВСЮ секцію Contact (від `<a class="anchor" id="contact">` до `<!--/contact-->`)
3. Вставити код з артефакту `updated_contact_section`

#### 3.3. Завантажити на сервер
```bash
# Якщо використовуєте git:
git add .
git commit -m "Add Telegram booking integration"
git push

# Або завантажте файли через FTP/SSH
```

---

## 🧪 Крок 4: Тестування

### Тест 1: Швидкі кнопки
1. Відкрити https://violin.pp.ua/
2. Прокрутити до секції "Services proposés"
3. Натиснути "Demander un Devis" на будь-якій картці
4. Вибрати "OK" → має відкритися Telegram з готовим повідомленням ✅

### Тест 2: Форма замовлення
1. Натиснути "Annuler" на попередньому кроці (або прокрутити до Contact)
2. Заповнити всі 4 кроки форми
3. Натиснути "Envoyer la demande"
4. Арсен має отримати повідомлення в Telegram ✅

### Тест 3: Перевірка повідомлення
Повідомлення в Telegram має виглядати так:
```
🎭 NOUVELLE DEMANDE: Concert

👤 Contact:
   Jean Dupont
   jean@email.com
   +41 79 123 4567

📅 Date: 15 mars 2025
📍 Lieu: Lausanne

💬 Détails:
   Musique classique pour mariage, 2 heures

🌐 Langue: 🇫🇷 Français
⏰ Reçu: 22/11/2025 15:30:45
```

---

## 🎨 Бонус: Налаштування бота (опціонально)

Через @BotFather можна:

### 1. Змінити фото профілю
```
/setuserpic
→ Вибрати бота
→ Завантажити фото (наприклад, фото зі скрипкою)
```

### 2. Додати опис
```
/setdescription
→ Вибрати бота
→ Написати:
Бот для прийому замовлень концертів та уроків скрипки.
Bot pour recevoir les demandes de concerts et cours de violon.
```

### 3. Додати команди (для зручності)
```
/setcommands
→ Вибрати бота
→ Вставити:
help - Інформація про послуги
prices - Прайс-лист
contact - Контактна інформація
```

---

## 🔧 Налаштування після деплою

### Змінити тексти повідомлень
Якщо треба змінити тексти швидких повідомлень для Telegram:
- Відкрити `assets/js/main.js`
- Знайти об'єкт `quickMessages` (близько рядка 52)
- Змінити тексти французькою та українською

### Додати нові типи послуг
1. В `index.html` додати нову опцію в `<select id="service-type">`
2. В `main.js` додати нове повідомлення в `quickMessages`
3. В Worker додати emoji в `serviceEmojis`

---

## 📊 Моніторинг

### Перевірити логи Worker'а
1. В Cloudflare Dashboard → Worker → **Logs**
2. Бачити всі запити та помилки в реальному часі

### Статистика
Cloudflare показує:
- Кількість запитів
- Успішні/неуспішні відправки
- Час відповіді

---

## ❗ Troubleshooting

### Проблема: "Erreur lors de l'envoi"
**Рішення:**
1. Перевірити, чи правильний `WEBHOOK_URL` в `main.js`
2. Перевірити Worker Logs в Cloudflare
3. Перевірити, чи додані Environment Variables

### Проблема: Бот не відправляє повідомлення
**Рішення:**
1. Перевірити `TELEGRAM_CHAT_ID` (має бути числом, без лапок)
2. Переконатися, що Арсен написав боту хоч раз (бот не може писати першим)
3. Перевірити TOKEN через: 
   ```
   https://api.telegram.org/bot8242972523:AAHrhraZ2dir1Arn32nLSnPPT1RZe9XSVuQ/getMe
   ```

### Проблема: Кнопки не працюють
**Рішення:**
1. Очистити кеш браузера (Ctrl+Shift+R)
2. Перевірити консоль браузера (F12 → Console)
3. Перевірити, чи `main.js` завантажився правильно

---

## 🎉 Готово!

Після виконання всіх кроків:
- ✅ Відвідувачі можуть швидко писати в Telegram
- ✅ Або заповнити детальну форму
- ✅ Арсен отримує красиві структуровані повідомлення
- ✅ Все безкоштовно (Cloudflare Free tier)

**Питання?** Пишіть, допоможу налаштувати! 🚀